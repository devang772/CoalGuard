fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
sqlalchemy>=2.0.30,<3.0
psycopg[binary]>=3.2
pydantic>=2.7,<3.0
pydantic-settings>=2.4
pyjwt>=2.9
bcrypt>=4.2
python-multipart>=0.0.9
pillow>=10.4          # read photos + EXIF
imagehash>=4.3        # look-alike photo fingerprint (perceptual hash)
shapely>=2.0          # is a GPS point inside the mine boundary
apscheduler>=3.10,<4  # timed jobs: reminders, escalations, nightly tasks
tzdata>=2024.1        # Indian time zone data (needed on Windows)
cloudinary>=1.40      # optional file storage (STORAGE_BACKEND=cloudinary)
fpdf2>=2.7            # PDF reports
openpyxl>=3.1         # Excel reports

# AI / ML analytics and regulatory ingestion
numpy>=2.0
pandas>=2.2
scikit-learn>=1.5
xgboost>=2.1
joblib>=1.4
python-dotenv>=1.0
pdfplumber>=0.11
faster-whisper>=1.0   # offline speech-to-text for voice reports (model: app/ai/models/whisper)

# testing
pytest>=8.2
httpx>=0.27
